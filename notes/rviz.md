Don't forget to add the TF component in RVIZ and select the fixed frame.

You can also add a RobotModel component.

If the latest changes after a restart of `robot_state_publisher` aren't visualized you can press the reset button, or close and reopen.